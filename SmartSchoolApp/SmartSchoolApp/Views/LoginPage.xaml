<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SmartSchoolApp.Views.LoginPage" Title="Login">
    <StackLayout HorizontalOptions="FillAndExpand" Spacing="20" Padding="20" Orientation="Vertical" VerticalOptions="CenterAndExpand">
  
        <Entry Placeholder="Username"  PlaceholderColor="gray" />
        <Entry Placeholder="Password"  PlaceholderColor="gray" IsPassword="True" />
        <Button Text="Login"  CornerRadius="5"/>
         <Grid Margin="0,10,0,10" VerticalOptions="End">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" VerticalOptions="Center" HorizontalOptions="End" Text="New User?"></Label>
                <Button Grid.Row="0" Grid.Column="1" Text="Register" Command="{Binding RegisterCommand}" VerticalOptions="Center" CornerRadius="5"/>
            </Grid>
        </StackLayout>
        
</ContentPage>